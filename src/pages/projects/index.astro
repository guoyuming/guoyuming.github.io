---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const projects = await getCollection('projects');

const sortedProjects = projects.sort((a, b) => {
  if (a.data.order !== undefined && b.data.order !== undefined) {
    return a.data.order - b.data.order;
  }
  if (a.data.order !== undefined) return -1;
  if (b.data.order !== undefined) return 1;
  return a.data.title.localeCompare(b.data.title, 'zh-CN');
});
---

<BaseLayout title="Project">
  <h2 class="section-heading" data-i18n="project">项目</h2>

  {sortedProjects.length === 0 ? (
    <p class="empty-state">Coming soon.</p>
  ) : (
    <ul class="project-grid">
      {sortedProjects.map((project) => {
        const coverImage = project.data.cover || `/images/projects/${project.slug}/cover.png`;
        return (
          <li class="browser-frame">
            <a href={`/projects/${project.slug}`} class="browser-frame-link">
              <div class="browser-topbar"></div>
              <div class="browser-content">
                <img
                  src={coverImage}
                  alt={project.data.title}
                  class="browser-preview"
                  loading="lazy"
                />
                <div class="project-label">{project.data.title}</div>
              </div>
            </a>
          </li>
        );
      })}
    </ul>
  )}
</BaseLayout>

<style>
  .project-grid {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 28px;
    max-width: var(--max-width, 1440px);
    margin: 0 auto;
  }

  /* ---- Browser Frame Card (Light default) ---- */
  .browser-frame {
    border-radius: 12px;
    overflow: hidden;
    background: #EAEAEA;
    border: 1px solid var(--color-border);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .browser-frame:hover {
    transform: scale(1.015);
    border-color: var(--color-border-hover);
    box-shadow: var(--card-shadow);
  }

  .browser-frame-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  /* ---- Top Bar ---- */
  .browser-topbar {
    height: 28px;
    background: #F3F3F3;
    display: flex;
    align-items: center;
    padding: 0 12px;
  }

/* ---- Content Area ---- */
  .browser-content {
    position: relative;
    background: #F8F8F8;
    aspect-ratio: 16 / 9;
  }

  .browser-preview {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* ---- Project Name Label ---- */
  .project-label {
    position: absolute;
    bottom: 0;
    left: 0;
    padding: 4px 12px;
    background: rgba(255, 255, 255, 0.8);
    color: #333333;
    font-family: var(--font-heading, 'Instrument Sans', sans-serif);
    font-size: 0.875rem;
    font-weight: 500;
    line-height: 1.4;
    border-top-right-radius: 4px;
  }

  .empty-state {
    text-align: center;
    padding: 80px 20px;
    color: var(--color-text-muted);
    font-size: 1.1rem;
  }

  /* ---- Dark Theme Override ---- */
  :global([data-theme="dark"]) .browser-frame {
    background: #2D2D2D;
    border-color: rgba(255, 255, 255, 0.08);
  }

  :global([data-theme="dark"]) .browser-frame:hover {
    border-color: rgba(255, 255, 255, 0.18);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }

  :global([data-theme="dark"]) .browser-topbar {
    background: #2D2D2D;
  }

  :global([data-theme="dark"]) .browser-content {
    background: #363638;
  }

  :global([data-theme="dark"]) .project-label {
    background: rgba(0, 0, 0, 0.6);
    color: #FFFFFF;
  }

  /* ---- Responsive ---- */
  @media (max-width: 768px) {
    .project-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }
</style>
